# Network Topology

This file is kept as a short compatibility note.

The primary networking guide is now [docs/networking.md](/home/geraldebmer/repos/local-agent-devstack/docs/networking.md).

Current default:

- laptop reaches workstation services over Tailscale
- workstation hosts the Docker stack
- SSH tunnels remain optional fallback only
